function [chiS, LS] = f_arclength(traj)
%F_ARCLENGTH

LS(1) = 0;
for i = 2:length(traj)
    x = traj(1, i) - traj(1, i-1);
    y = traj(2, i) - traj(2, i-1);
    z = traj(3, i) - traj(3, i-1);
    S(i) = sqrt(x^2 + y^2 + z^2);
    LS(i) = LS(i-1) + S(i);
end

for 1 = 1:length(traj)
    VS(i) = 0.25 * (traj(1, i)^2)

end
chiS = 0;
end

